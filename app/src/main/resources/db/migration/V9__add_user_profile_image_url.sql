-- =============================================================================
-- V9: 회원 프로필 이미지 URL 컬럼 추가 (이슈 #85)
--   - users.profile_image_url: S3 퍼블릭 URL 저장 (미설정 시 NULL)
-- =============================================================================

ALTER TABLE users
    ADD COLUMN profile_image_url VARCHAR(500);
