# 로컬 개발용 시드

이 디렉터리의 SQL은 로컬 프로파일에서만 실행한다.

- 파일명은 Flyway 반복 마이그레이션 형식인 `R__local_{설명}.sql`을 사용한다.
- 다시 실행되어도 결과가 달라지지 않도록 멱등성을 보장한다.
- 운영에 필요한 기준 데이터나 스키마 변경은 이곳에 추가하지 않는다.
- 실제 사용자 정보, 운영 데이터, 시크릿은 포함하지 않는다.
